import {Grammar} from './grammar'

/**
 * Проверка достижимости нетерминалов
 */
const checkReachable = (grammar: Grammar, start: string): void => {
}

/**
 * Проверка продуктивности нетерминалов
 */
const checkProductive = (grammar: Grammar): void => {
}

/**
 * Проверка однозначности (LL(1)-условие)
 */
const checkLL1 = (grammar: Grammar, start: string): void => {
}


export {
	checkReachable,
	checkProductive,
	checkLL1,
}